{
  "model": "gemini-3.7-flash",
  "enableTerminalSandbox": false,
  "permissions": {
    "mode": "request-review",
    "allow": [
      "command(git log)",
      "command(git status)",
      "command(git diff)",
      "command(npm test)",
      "command(npm run lint)"
    ]
  }
}
